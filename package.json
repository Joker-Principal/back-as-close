{
  "name": "back-as-close",
  "version": "1.2.0",
  "private": true,
  "description": "Chrome extension that closes the current tab when Back has no history.",
  "scripts": {
    "clean": "node -e \"require('fs').rmSync('dist', { recursive: true, force: true })\"",
    "build": "npm run clean && tsc && node scripts/copy-manifest.mjs",
    "watch": "tsc --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.14",
    "typescript": "^5.9.2"
  }
}